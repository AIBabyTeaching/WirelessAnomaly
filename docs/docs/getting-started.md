# Getting Started

Install the package and run the reduced pipeline on the included sample data:

```bash
pip install -e .
wireless-anom run reduced --config src/wireless_anom/config/defaults.yaml
```
