# Reproduce

Use the provided Makefile target to regenerate all figures and tables used in
the paper:

```bash
make reproduce
```

Individual figures can be created via the CLI, e.g.

```bash
wireless-anom viz figures --config src/wireless_anom/config/defaults.yaml
```
